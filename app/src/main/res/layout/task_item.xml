<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="viewModel"
            type="io.aceisnotmycard.doit.viewmodel.ListItemViewModel"/>
    </data>

    <android.support.v7.widget.CardView
        android:id="@+id/task_item_view"
        android:layout_width="match_parent"
        android:layout_height="@dimen/list_item_height"
        android:layout_marginLeft="4dp"
        android:layout_marginRight="4dp"
        android:layout_marginTop="2dp"
        android:layout_marginBottom="2dp"
        app:cardCornerRadius="2dp"
        android:elevation="@dimen/very_small_elevation"
        app:cardBackgroundColor="@{viewModel.important ? @color/colorAccent : @color/white}">

        <TextView
            android:id="@+id/task_item_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:layout_margin="8dp"
            android:textSize="@dimen/normal_text_size"
            android:text="@{viewModel.title}" />

    </android.support.v7.widget.CardView>
</layout>